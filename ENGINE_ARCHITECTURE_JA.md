# 魔法将棋AIの仕組み

## 全体の流れ

1. Reactは現在の盤面・持ち駒・手番を探索用Workerへ送る。
2. Worker内のWASMエンジンが合法手を列挙する。
3. エンジンは反復深化で浅い読みから始め、前回有望だった手を先に読む。
4. 各手を指した局面を作り、相手の最善応手、自分の最善応手……と読む。
5. 詰みまたは安全なトライは通常の評価値より上位の確定勝ちとして扱う。
6. 時間内に最後まで完了した深度の最善手をWorkerがReactへ返す。

画面のアニメーション中にもWorkerは独立して探索できる。表示処理と探索処理を
分離しているため、駒の移動や音声で画面が止まっていても思考時間に利用できる。

## 一般的な将棋AIと同じ部分

- **反復深化**: 深度1、2、3……と読み、常に指せる暫定手を保持する。
- **αβ探索**: 結果に影響しない枝を打ち切る。
- **置換表**: 同じ局面の再計算を避ける。
- **手の並べ替え**: 前回の最善手、駒取り、王手などを先に読む。
- **PVS**: 最有力手以外を狭い窓で確認し、必要な枝だけ読み直す。
- **LMR**: 後順位の静かな手を一旦浅く読み、良さそうなら元の深度で読み直す。
- **静止探索**: 深度末端が駒の取り合い中なら、局面が落ち着くまで延長する。

「7手読む」は全合法手を同じ量だけ総当たりする意味ではない。有力な枝や王手・
駒取りは深く、明らかに悪い枝は安全な根拠を持って早く打ち切る。この差が通常の
将棋AIの速度を生む。

## 魔法将棋で追加する部分

### 合法手

- 6×6盤
- 5種類の持ち駒と打ち場所制限
- 敵陣2段目以降での強制魔女化
- ナノカの「動かずに捕獲」
- ヒロの「味方との入替」
- マーゴの「敵を飛び越して捕獲」
- ハンナの「途中の駒を無視する2マス移動」

### 勝敗

- エマを詰ませれば勝ち
- エマが相手最下段へ到達し、相手に取られない状態ならトライ勝ち
- 同一局面3回は千日手

トライは評価値のボーナスではなく、詰みと同じ終端判定にする。したがって
「トライできそう」という雰囲気だけで駒を捨てることを防ぎ、相手の全合法手で
阻止できないことを確認して初めて勝ちになる。

## なぜ特殊手の実装が広範囲になるか

探索エンジンは着手ごとに盤面を進め、探索後に完全に元へ戻す。特殊手も次の全処理
で同じ意味にならなければならない。

| 処理 | 必要な対応 |
| --- | --- |
| 合法手生成 | 特殊手を候補として生成する |
| 王手判定 | 特殊能力でエマを取れるか判定する |
| make | 駒移動・捕獲・持ち駒化・成りを反映する |
| undo | 盤面と持ち駒を1ビットも違わず戻す |
| 局面ハッシュ | 特殊手後の局面を正しく識別する |
| 棋譜表現 | Reactが演出種別を判別できる形で返す |

盤面表示だけ特殊手にすると、AI内部では別のゲームを読むことになる。このため、
Fairy-Stockfish本体の着手表現から一貫して追加する。

## 現行Rust版とFairy-Stockfish版

`public/fairy`にビルド済み本体があればFairy-Stockfishを優先する。まず深度7を必ず
完走し、残った思考時間で反復深化を続ける。本体が未生成または起動不能の場合だけ、
現行Rust/WASM版、最後にJavaScript版へ自動的に戻る。

Reactの局面はFENへ変換してUCI Workerへ渡す。返ってきた`bestmove`は既存UIの
アクションへ戻すため、銃撃・入替・跳躍・浮遊のアニメーションや音声は従来通り使える。

Fairy-StockfishはGPL-3.0-or-laterであるため、配布するWASMと対応する改造ソースを
同時に公開する。
